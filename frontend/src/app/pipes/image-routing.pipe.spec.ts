import { ImageRoutingPipe } from './image-routing.pipe';

describe('ImageRoutingPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageRoutingPipe();
    expect(pipe).toBeTruthy();
  });
});
